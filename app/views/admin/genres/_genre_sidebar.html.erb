<!--customer/products/topにrenderを追加-->
<!--customer/products_controllerのtopアクションにgenreを定義-->
<table>
  <thead>
    <th>ジャンル検索</th>
  </thead>

  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to products_path(genre_id: genre.id) do %>
            <%= genre.name %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
 </table>
